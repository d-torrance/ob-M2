name: check

on:
  push:
    branches:
      - master

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: leotaku/elisp-check@master
        with:
          file: ob-macaulay2.el
